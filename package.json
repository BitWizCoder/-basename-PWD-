{
  "scripts": {
    "build": "postcss src/style.css -o dist/style.css --watch",
    "dev": "npm run build && live-server index.html --port=8080"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.49",
    "tailwindcss": "^3.4.17"
  },
  "dependencies": {
    "postcss-cli": "^11.0.0"
  }
}